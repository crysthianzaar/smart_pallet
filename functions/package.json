{
  "name": "smart-pallet-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for SmartPallet",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "csv-writer": "^1.6.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^4.9.0",
    "handlebars": "^4.7.8",
    "puppeteer": "^21.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  },
  "private": true
}
